import ServerFetch from "@/components/server-fetch";

export default async function Home() {
  
  return (
      <main>
        <h1>Homesdss</h1> 
        {/* <Acesso/>        */}
        <ServerFetch/>
      </main>
  );
}
